---
import { SUPPORTED_LANGUAGES } from '../../utils/i18n';

export const getStaticPaths = () => {
  return SUPPORTED_LANGUAGES.map(lang => ({
    params: { lang },
  }));
};

const { lang } = Astro.params as { lang: 'en' | 'de' };
console.log('Current language:', lang); // Use lang for locale-specific rendering
// Use 'lang' to filter and render content for the current locale
// Example: const entries = await getCollection('books', lang);
// ...existing code...
---
// ...existing code...